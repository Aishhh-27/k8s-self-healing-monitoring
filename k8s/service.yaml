apiVersion: v1
kind: Service
metadata:
  name: self-healing-service
spec:
  selector:
    app: self-healing-app   # MUST MATCH DEPLOYMENT
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
